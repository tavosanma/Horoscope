<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cl.ubiobio.horoscopo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".acuario" />
        <activity android:name=".aries" />
        <activity android:name=".cancer" />
        <activity android:name=".capricornio" />
        <activity android:name=".escorpio" />
        <activity android:name=".geminis" />
        <activity android:name=".leo" />
        <activity android:name=".libra" />
        <activity android:name=".piscis" />
        <activity android:name=".sagitario" />
        <activity android:name=".tauro" />
        <activity android:name=".virgo"></activity>
    </application>

</manifest>